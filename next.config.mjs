/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'images.unsplash.com',
            'img-s-msn-com.akamaized.net',
            'th.bing.com'
        ]
    }
};

export default nextConfig;
